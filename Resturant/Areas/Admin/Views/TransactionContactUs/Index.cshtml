@model IEnumerable<Resturant.Models.TransactionContactUs>

@{
    ViewData["Title"] = "Index";
}

<h1>Transaction Contact Us</h1>


 



<main class="main">

    <div class="pagetitle d-flex justify-content-between">

        <div>
            <h1>Index</h1>
            <nav>
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Home">Home</a></li>
                    <li class="breadcrumb-item">Transaction Contact Us</li>


                </ol>

            </nav>
        </div>



        @* <p>
            <a class="btn btn-primary" asp-action="Create">
                <i class=" ri-add-circle-fill me-1 align-middle"></i>
                <span class="me-1 align-middle"> Create New</span>
            </a>
        </p> *@






    </div><!-- End Page Title -->

    <section class="section">
        <div class="row">
            <div class="col-lg-12">

                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title"></h5>


                        <!-- Table with stripped rows -->
                        <table class="table datatable">
                            <thead>
                                <tr>
                                    <th>
                                        @Html.DisplayNameFor(model => model.TransactionContactUsFullName)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.TransactionContactUsEmail)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.TransactionContactUsSubject)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.TransactionContactUsMessage)
                                    </th>
                                    
                                
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.TransactionContactUsFullName)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.TransactionContactUsEmail)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.TransactionContactUsSubject)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.TransactionContactUsMessage)
                                        </td>
                                         
                                        @* <td>
                                            <a asp-action="Edit" asp-route-id="@item.TransactionContactUsId">Edit</a> |
                                            <a asp-action="Details" asp-route-id="@item.TransactionContactUsId">Details</a> |
                                            <a asp-action="Delete" asp-route-id="@item.TransactionContactUsId">Delete</a>
                                        </td> *@
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <!-- End Table with stripped rows -->

                    </div>
                </div>

            </div>
        </div>
    </section>

</main>